{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "@rethunk/mcp-multi-root-git: git_status",
  "description": "Parameter schema for the 'git_status' MCP tool.",
  "type": "object",
  "properties": {
    "root": {
      "description": "Repo path, array of paths, or \"*\" for all MCP roots.",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    },
    "includeSubmodules": {
      "default": true,
      "type": "boolean"
    },
    "maxChangedFiles": {
      "default": 500,
      "description": "Cap changed-file lines per repo (hard cap 20000).",
      "type": "integer",
      "minimum": 1,
      "maximum": 20000
    },
    "maxSubmodules": {
      "default": 64,
      "description": "Cap submodules fanned out to per root (hard cap 256).",
      "type": "integer",
      "minimum": 1,
      "maximum": 256
    }
  },
  "required": [
    "includeSubmodules",
    "maxChangedFiles",
    "maxSubmodules"
  ],
  "additionalProperties": false
}
