{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "@rethunk/mcp-multi-root-git: git_blame",
  "description": "Parameter schema for the 'git_blame' MCP tool.",
  "type": "object",
  "properties": {
    "workspaceRoot": {
      "description": "Repo path. Default: first MCP root / cwd.",
      "type": "string"
    },
    "path": {
      "type": "string",
      "minLength": 1,
      "description": "Repo-relative path to the file to blame."
    },
    "ref": {
      "description": "Optional commit-ish (SHA, branch, tag) to blame at. Ancestor notation is accepted (e.g. \"HEAD~3\", \"main^2\").",
      "type": "string"
    },
    "startLine": {
      "description": "First line of the range to blame (1-based). Requires endLine.",
      "type": "integer",
      "minimum": 1,
      "maximum": 1000000
    },
    "endLine": {
      "description": "Last line of the range to blame (1-based, inclusive). Requires startLine.",
      "type": "integer",
      "minimum": 1,
      "maximum": 1000000
    },
    "maxLines": {
      "default": 2000,
      "description": "Max blamed lines to return. Default: 2000.",
      "type": "integer",
      "minimum": 1,
      "maximum": 10000
    },
    "ignoreWhitespace": {
      "default": false,
      "description": "Ignore whitespace-only changes when assigning blame (`-w`).",
      "type": "boolean"
    },
    "detectMoves": {
      "default": false,
      "description": "Detect lines moved or copied within the same file (`-M`).",
      "type": "boolean"
    },
    "detectCopies": {
      "default": false,
      "description": "Detect lines moved or copied from other files in the same commit (`-C`).",
      "type": "boolean"
    }
  },
  "required": [
    "path",
    "maxLines",
    "ignoreWhitespace",
    "detectMoves",
    "detectCopies"
  ],
  "additionalProperties": false
}
