{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "@rethunk/mcp-multi-root-git: git_revert",
  "description": "Parameter schema for the 'git_revert' MCP tool.",
  "type": "object",
  "properties": {
    "workspaceRoot": {
      "description": "Repo path. Default: first MCP root / cwd.",
      "type": "string"
    },
    "format": {
      "default": "json",
      "type": "string",
      "enum": [
        "markdown",
        "json"
      ]
    },
    "sources": {
      "minItems": 1,
      "maxItems": 20,
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Commits to revert, applied in order: SHA, branch/tag name, or ancestor notation (`HEAD~1`)."
    },
    "noCommit": {
      "default": false,
      "description": "Pass `--no-commit`: apply the revert(s) to the index/working tree without committing (changes are left staged instead).",
      "type": "boolean"
    },
    "mainline": {
      "description": "Parent number (`-m N`) to diff against — required when reverting a merge commit.",
      "type": "integer",
      "minimum": 1,
      "maximum": 9007199254740991
    },
    "onConflict": {
      "default": "abort",
      "description": "`abort` (default): on conflict, run `revert --abort` and roll back the whole in-progress revert sequence (unchanged behavior). `pause`: on conflict, leave the conflict and native revert sequencer state in place — commits already made stay applied — so it can be resolved and resumed via `git_revert_continue`.",
      "type": "string",
      "enum": [
        "abort",
        "pause"
      ]
    }
  },
  "required": [
    "format",
    "sources",
    "noCommit",
    "onConflict"
  ],
  "additionalProperties": false
}
